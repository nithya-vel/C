int removeDuplicates(int* nums, int numsSize) {
    int last = -999;
    int index =0;
    int temp[numsSize];
    for(int i =0;i<numsSize;i++){
        if(nums[i] != last){
            nums[index] = nums[i];
            last = nums[i];
            index++;
        }
    }
return index;
}
