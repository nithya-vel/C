The input is a sorted array of distinct integers and a target value.
The algorithm must return the index of the target if it is found in the array.
If the target is not found, the algorithm must return the index where it would be inserted to maintain the sorted order.
